#include <stdio.h>

void main()
{
	float s[10],max,min,sum,sorce;
	int i;
	for (i = 0;i<10;i++) 
	scanf("%f",&s[i]);
	max = min = sum = s[0];
	for (i = 0;i<10;i++) 
	{
        max >= s[i] ? : max = s[i];
	}
}
